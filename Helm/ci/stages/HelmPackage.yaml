parameters:
- name: Environment
  type: string

stages:
- stage: HelmPackage
  variables:
  - group: "$(variables_group)"
  pool: $(pool)
  displayName: 'Helm package'
  jobs:
  - template: ../jobs/HelmPackage.yaml
    parameters:
      Environment: ${{ parameters.Environment }}