parameters:
- name: Environment
  type: string

stages:
- stage: HelmPackage
  variables:
  - group: "${{ variables.variables_group }} Environment Variable Group"
  pool: $(pool)
  displayName: 'Helm package'
  jobs:
  - template: ../jobs/HelmPackage.yaml
    parameters:
      Environment: ${{ parameters.Environment }}